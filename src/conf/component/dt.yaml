name: dt
mode: train
batch_size: 32
num_iterations: 5000
val_interval: 10
max_context_length: 96
dataset:
  mode: global
  val_list: []
  sliding_window_size: 1344
  sliding_window_offset: 1344
  forecast_horizon: 32
optimizer:
  lr: 1e-4
  weight_decay: 1e-4
transformer:
  model: basic
  num_heads: 4
  num_layers: 12
model_dim: 512
target_return:
  val: 50
  test: 200
early_stopping_patience: 500